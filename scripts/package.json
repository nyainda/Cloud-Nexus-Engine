{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check-schema": "tsx src/check-schema.ts",
    "smoke-test": "tsx src/smoke-test.ts",
    "full-test": "tsx src/full-test.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "xlsx": "^0.18.5"
  }
}
