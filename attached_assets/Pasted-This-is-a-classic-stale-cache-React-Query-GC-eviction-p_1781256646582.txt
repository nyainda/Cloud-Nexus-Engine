This is a classic stale cache + React Query GC eviction problem. Let me trace the exact chain before touching anything.Ran 4 commandsRan 4 commandsNow I have the full picture. Let me also check the AuthGuard's session validation and the RecentSalesDrawer fetch issue:Read the AuthGuard session validation and prefetch logicRead the AuthGuard session validation and prefetch logicGot it. I've found all three bugs causing this. Let me write the exact fixes.
The root causes are:
Bug 1 — Products disappear after idle: gcTime: 30 * 60_000 (30 min) but React Query evicts cached data from memory when the component unmounts. When the PWA goes to background on mobile/desktop, the OS may freeze the JS engine. When you come back, components remount but the refetchInterval was paused, so the query re-fires — and until it returns (~500ms), React Query shows undefined = blank screen. The gcTime doesn't protect you because the query has zero observers while the app was backgrounded.
Bug 2 — Cash sale goes through twice: The handleCheckout function clears the cart optimistically and fires createSale.mutate(). If the network is slow (CF Worker cold start = up to 3s), the cashier taps again. createSale.isPending is set, so the button is disabled — but the disabled prop is on CartPanel which is already gone (cart was cleared). The cart was already cleared optimistically, so they re-add items and tap again. Two real sales hit the server. This is the dangerous one.
Bug 3 — RecentSalesDrawer uses raw customFetch returning Response but then calls .ok and .json() on it — but customFetch already parses the body and throws on error, so res.ok is always undefined and res.json() fails. This means sales history silently errors.Ran 2 commandsRan 2 commandsNow I have everything. Let me write all three fixes:Backup files before editingBackup files before editingFix 1 — App.tsx: refetchOnWindowFocus: true + refetchOnReconnect: true so returning to the app immediately refetches instead of showing blankFix QueryClient config: enable refetchOnWindowFocus and refetchOnReconnect so returning from idle/background always refreshes dataFix QueryClient config: enable refetchOnWindowFocus and refetchOnReconnect so returning from idle/background always refreshes dataFix 2 — pos.tsx: Add visibilitychange listener to force-refetch products when app resumes, and fix the double-sale bug with a submitting ref guard
Fix 3 — pos.tsx: Fix RecentSalesDrawer — customFetch already returns parsed data, remove the .ok/.json() callsAdd visibilitychange refetch + fix RecentSalesDrawer customFetch usage (it already parses the body)Add visibilitychange refetch + fix RecentSalesDrawer customFetch usage (it already parses the body)Now fix the double-sale bug — add a submitting ref guard so the cashier can never double-fire:Edited 3 filesEdited 3 filesAlso need to release the lock for the offline path:Release lock on offline pathRelease lock on offline pathNow fix the RecentSalesDrawer — customFetch already parses and throws, the .ok/.json() pattern is wrong:Fix RecentSalesDrawer to use customFetch correctly - it already parses the body and throws on errorsFix RecentSalesDrawer to use customFetch correctly - it already parses the body and throws on errors
Sonnet 4.6 LowClaude is AI and can make mistakes. Please double-check responses.