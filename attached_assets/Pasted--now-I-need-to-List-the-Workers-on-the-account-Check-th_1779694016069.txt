 now.

I need to:

List the Workers on the account
Check their CPU metrics via GraphQL
Let me start by listing Workers and querying their CPU metrics.There's one Worker: greenlink-pos-api. Let me now query the Workers analytics to get CPU time data for this worker over the last 24 hours.The script is very long and truncated. Let me analyze what I have so far and also look at the CPU metrics more carefully. Let me calculate the average CPU time per request.

From the wallTime data (in nanoseconds):

2026-05-24T07: success: 109 req, 41817168 ns → avg ~383,680 ns ≈ 0.38 ms per req
2026-05-24T08: success: 15 req, 2593240 ns → avg ~172,883 ns ≈ 0.17 ms
2026-05-24T09: 71 req, 24892044 ns → avg ~350,592 ns ≈ 0.35 ms
2026-05-24T10: 6 req, 1432871 ns → avg ~238,812 ns ≈ 0.24 ms
2026-05-24T11: 55 req, 47144803 ns → avg ~857,178 ns ≈ 0.86 ms
2026-05-24T12: 113 req, 26908367 ns → avg ~238,126 ns ≈ 0.24 ms
2026-05-24T13: 118 req, 49698563 ns → avg ~421,173 ns ≈ 0.42 ms
2026-05-24T14: 212 req, 33343046 ns → avg ~157,278 ns ≈ 0.16 ms
2026-05-24T15: 140 req, 17979962 ns → avg ~128,428 ns ≈ 0.13 ms
2026-05-24T16: 29 req, 9285465 ns → avg ~320,189 ns ≈ 0.32 ms
2026-05-24T17: 152 req, 31694213 ns → avg ~208,514 ns ≈ 0.21 ms
2026-05-24T18: 287 req, 74309092 ns → avg ~258,916 ns ≈ 0.26 ms
2026-05-25T03: 29 req, 39880292 ns → avg ~1,375,182 ns ≈ 1.38 ms
2026-05-25T04: 32 req, 43203983 ns → avg ~1,350,124 ns ≈ 1.35 ms
2026-05-25T05: 19 req, 5498553 ns → avg ~289,397 ns ≈ 0.29 ms
2026-05-25T06: 67 req, 53620442 ns → avg ~800,305 ns ≈ 0.80 ms
Wait, the user said "CPU is too high" and wants to be "under 10". This likely refers to the Workers CPU time limit. On the free plan, Workers have a 10ms CPU time limit per request. On the paid plan (Workers Bundled/Unbound), it's 30ms/30s.

Let me recalculate. The wallTime is in nanoseconds. Let me check the early morning hours more carefully:

2026-05-25T03: 29 req, 39,880,292 ns = ~39.9 ms total → avg ~1.38 ms per req
2026-05-25T04: 32 req, 43,203,983 ns = ~43.2 ms total → avg ~1.35 ms per req
Hmm, but these averages are well under 10ms. But the user is saying CPU is too high. Maybe they're hitting the CPU limit on some requests? Or maybe they're referring to something else?

Wait, let me reconsider. The user might be referring to the CPU time limit on the free plan which is 10ms. Some individual requests might be exceeding 10ms CPU time. The wallTime includes I/O wait time, so it's not the same as CPU time. But if wallTime is already close to 10ms for some requests, CPU time could be hitting the limit.

Actually, I should look at this differently. The user might be seeing errors related to exceeding the 10ms CPU limit on the free plan. Let me check if there are any exceeded errors.

Actually, the analytics showed 0 errors. But the user is saying CPU is too high. Let me look at the script more carefully - it's a bundled app (looks like a Nuxt/H3 app based on the unenv imports). These frameworks can be CPU-heavy due to routing, middleware, etc.

Let me search for the actual application logic in the script. The truncated script is mostly framework/polyfill code. Let me try to get the settings/bindings to understand more.

Actually, let me focus on what I can determine:

The Worker greenlink-pos-api is a bundled Node.js-compatible app (using unenv, H3/Nuxt-style framework)
Average wall time per request varies from ~0.13ms to ~1.38ms
Some hours show higher average wall times, especially early morning hours (03:00, 04:00) with ~1.35ms avg
But these are averages - individual requests could be much higher
The key issue is likely:

This is a Node.js-compatible app running on Workers, which adds overhead from polyfills
The free plan has a 10ms CPU time limit
Some requests might be hitting or approaching this limit
Let me check the Worker's settings and also search documentation for CPU optimization tips.

Let me also check if there are any specific CPU time metrics available in the analytics. The wallTime isn't the same as CPU time - wall time includes time waiting for I/O (like subrequests). CPU time is the actual processing time.

Let me search the docs for CPU time limits and optimization, and also try to get more details about the worker.


see thinking
Let me actually pull the data now — I'll check your Workers and their CPU usage.You have one Worker: greenlink-pos-api. Let me pull its CPU time metrics.Let me get more details about the Worker's configuration and check for CPU-specific analytics.Now let me search for the documentation on how to profile and optimize CPU usage.Now I have a clear picture. Let me visualize the data and give you the analysis.Your Worker greenlink-pos-api has high CPU usage because its average wall time per request is ~356 ms — far above the 10 ms CPU limit on the Free plan. Some hours spike to over 1,300 ms per request.

Here's what I found and how to fix it:

The Problem
Metric	Your Worker	Target (Free plan)	Typical Worker
Avg wall time/request	356 ms	< 10 ms CPU	~2.2 ms CPU
Your Worker uses: D1 database, KV namespace (SESSIONS), Gemini AI API, and VAPID push notifications. The wall time is dominated by waiting on these I/O calls, but the CPU time (actual computation) is likely also high due to:

Gemini AI API calls — processing/parsing large AI responses is CPU-intensive
Heavy framework overhead — your bundle uses unenv + nodejs_compat, adding significant startup and runtime overhead
JSON parsing of large payloads — AI responses and D1 result sets can be large
VAPID/JWT signing — cryptographic operations for push notifications are CPU-expensive
How to Get Under 10 ms CPU Time
1. Profile your code locally (most important)
Run wrangler dev, press D to open DevTools, go to the Profiler tab, and record a CPU profile while sending requests. This shows exactly which functions burn CPU time.

2. Upgrade to Workers Paid plan
The Free plan has a hard 10 ms CPU limit. Even well-optimized Workers with AI calls or crypto operations often exceed this. The Paid plan ($5/mo) gives you 30 seconds of CPU time by default (up to 5 minutes), which your Worker would comfortably fit within.

3. Optimize your code
Cache AI responses in KV — avoid calling Gemini on every request if the same query is repeated
Stream AI responses — use Response streaming instead of buffering the entire AI output before responding
Reduce JSON parsing — only parse what you need from D1 results
Minimize framework overhead — if possible, use lighter routing instead of the full unenv/node compat layer
Cache computed values — reuse session validation results instead of re-verifying on every request
4. Set a CPU limit in wrangler config
To prevent runaway costs on the Paid plan:

[limits]
cpu_ms = 50
Bottom line: Your Worker's workload (AI API + D1 + crypto) is fundamentally too heavy for the 10 ms Free plan CPU limit. The fastest path forward is upgrading to the Paid plan, then profiling and optimizing to reduce costs. Would you like me to help with either step?