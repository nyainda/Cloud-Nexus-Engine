{
  "nodeVersion": "22.x",
  "installCommand": "npm config set registry https://registry.npmjs.org && pnpm config set registry https://registry.npmjs.org && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/greenlink-app build",
  "outputDirectory": "artifacts/greenlink-app/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
