auto-install-peers=false
strict-peer-dependencies=false
node-linker=hoisted
# Never auto-update packages — only explicit pnpm update commands
save-exact=true
# Lock engine — Node 22 matches Replit environment
engine-strict=false
